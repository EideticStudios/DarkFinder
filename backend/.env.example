# DarkFinder data pipeline uses Google Earth Engine.
# No credentials are needed in .env — authentication is handled via:
#   earthengine authenticate
# Run this once before using `make download` or `make pipeline`.
