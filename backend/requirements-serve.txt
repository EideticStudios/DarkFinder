# Serve-time dependencies only (no pipeline/dev tooling).
# Used by the production Docker image; the pipeline uses requirements.txt.
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
rasterio>=1.4.0
numpy>=2.0.0
rio-tiler>=7.0.0
pyproj>=3.7.0
pydantic>=2.10.0
Pillow>=10.0.0
